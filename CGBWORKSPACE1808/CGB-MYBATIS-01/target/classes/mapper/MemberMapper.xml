<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pt.member.dao.MemberDao">
	<!-- 基于id删除会员信息 -->
	<update id="deleteObject"
            parameterType="int">
        delete from t_members
        where id=#{6}
    </update>
	
	<!-- 更新密码 -->
	<update id="updatePassword">
        update t_members
        set password=#{password}
        where id=#{id}
    </update>

	<!-- 定义insert元素,其中parameterType可以不写
		ognl #{}表达式会调用参数对象的get方法
	 -->
	<insert id="insertObject" parameterType="com.pt.member.entity.Member">
		insert into t_members
		(nickname,password,realname,gender,rank,
		email,mobile,safequestion,safeanswer,createdTime)
	    values   
	  	(#{nickname},#{password},#{realname},#{gender},#{rank},
		#{email},#{mobile},#{safequestion},#{safeanswer},#{createdTime})
	</insert>
	<select id="findObjects" resultType="com.pt.member.entity.Member">
	         select * from t_members
	        
	</select>
	<!-- 查询会员信息:对于select元素而言,必须指定resultType -->
	<select id="findPageObjects" resultType="com.pt.member.entity.Member">
		 select * from t_members limit #{arg0},#{arg1}
	</select>

</mapper>
