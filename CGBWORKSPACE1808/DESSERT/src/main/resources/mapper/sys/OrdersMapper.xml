<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dessert.sys.dao.OrdersDao">
	<sql id="sqlWhereId">
		<where>
			<if test="number != null and number != ''">
				number like concat("%",#{number},"%")
			</if>
		</where>
	</sql>
	<select id="findPageObjects" resultType="com.dessert.sys.orders.entity.Orders">
		select
		o.id,u.username,o.number,o.createdTime,o.count,o.price,o.money,u.address,o.note
		from
		user u right join user_orders uo on(u.id=uo.user_id) right join
		orders o
		on(uo.orders_id=o.id)
		<include refid="sqlWhereId" />
		order by o.createdTime desc
		limit #{startIndex},#{pageSize}
	</select>
	<select id="getRowCount" resultType="int">
		select count(*) from orders
		<include refid="sqlWhereId" />
	</select>
	<delete id="deleteObjects">
		delete from orders where id in
		<foreach collection="ids" open="(" close=")" separator=","
			item="item">
			#{item}
		</foreach>
	</delete>

	<delete id="deleteObjectsByUserId">
		delete from orders where id in(select orders_id from
		user_orders where user_id = #{id})
	</delete>
</mapper>